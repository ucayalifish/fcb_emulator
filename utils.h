#ifndef FCB_EMULATOR_UTILS_H
#define FCB_EMULATOR_UTILS_H

#include <stddef.h>
#include <stdint.h>

void fill_random_buffer(unsigned seed, uint8_t * dest, size_t dest_size);

#endif //FCB_EMULATOR_UTILS_H
